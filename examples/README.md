# Optional Examples

This folder contains **reference patterns only**. Files here are **not** loaded by Cursor or agents by default.

Copy and adapt into `.agents/skills/`, `.agents/rules/`, or `AGENTS.md` when your project needs them.

| Example | Source inspiration | Copy to |
|---------|-------------------|---------|
| `optional-skills/caveman-response-style.md` | mini-framework `.agents/skills/caveman/` | `.agents/skills/caveman/SKILL.md` |
| `optional-rules/rtk-shell-wrapper.md` | mini-framework `.agents/rules/antigravity-rtk-rules.md` | `.agents/rules/rtk-shell.md` |

Do not reference these paths from core templates unless you have copied and enabled them.
